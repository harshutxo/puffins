{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); raw=$(printf '%s' \"$input\" | grep -o '\"file_path\"[[:space:]]*:[[:space:]]*\"[^\"]*\"' | head -1 | sed -E 's/.*:[[:space:]]*\"//; s/\"$//'); clean=$(printf '%s' \"$raw\" | tr -d '\\\\'); case \"$clean\" in C:Usersrealhpuffins*|/c/Users/realh/puffins/*) cd \"/c/Users/realh/puffins\" 2>/dev/null && git add -A 2>/dev/null; git commit -m \"auto: update via Claude Code\" >/dev/null 2>&1 || true; git push origin main >/dev/null 2>&1 || true ;; esac; exit 0",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
